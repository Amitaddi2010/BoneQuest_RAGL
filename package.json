{
  "name": "bonequest-v2",
  "private": true,
  "version": "2.0.0",
  "description": "BoneQuest v2 — PageIndex-Powered Orthopaedic RAG Platform",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "backend": "cd backend && python -m uvicorn main:app --reload --port 8000",
    "start": "concurrently \"npm run dev\" \"npm run backend\""
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "concurrently": "^9.0.0"
  },
  "dependencies": {
    "marked": "^15.0.0"
  }
}
